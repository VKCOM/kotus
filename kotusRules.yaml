aliases:
  #  if kotlin is used
  - alias: "kotlin"
    modules:
      - ":modules:three"
  - alias: "compose"
    modules: ":modules:three"
regexes:
  #  always include
  - regex: ".*"
    modules:
      - ":modules:four"